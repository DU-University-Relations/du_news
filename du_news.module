<?php

/**
 * Implements hook_help().
 */
function du_news_help_text($route_name, $route_parameters) {
  switch ($route_name) {
    case 'help.page.du_news':
      drupal_set_message(t('Custom News Package for Drupal 10'));
      break;
  }
}
